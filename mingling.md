# 命令练习

++***2026年3月25日23:00:03***++

git --version   查看版本号

git config --global “name”   设置用户名

git config --global “email”   设置邮箱





++***2026年3月27日16:45:34***++

ssh-keygen -t ed25519 -C “email”   生成SSH密钥

ssh-keygen -t ed25519 -C “email”   生成SSH密钥

ssh-keygen -t ed25519 -C “email”   生成SSH密钥

ssh-keygen -t ed25519 -C “email”   生成SSH密钥

ssh-keygen -t ed25519 -C “email”   生成SSH密钥

cat \~/.ssh/id_ed25519.pub   查看并复制公钥

cat \~/.ssh/id_ed25519.pub   查看并复制公钥

cat \~/.ssh/id_ed25519.pub   查看并复制公钥

cat \~/.ssh/id_ed25519.pub   查看并复制公钥

cat \~/.ssh/id_ed25519.pub   查看并复制公钥

ssh -T [git\@github.com](mailto:git@github.com)   测试连接

ssh -T [git\@github.com](mailto:git@github.com)   测试连接

ssh -T [git\@github.com](mailto:git@github.com)   测试连接

ssh -T [git\@github.com   测试连接](mailto:git@github.com)

ssh -T [git\@github.com](mailto:git@github.com)   测试连接

git init   初始化仓库

git init   初始化仓库

git init   初始化仓库

git status   查看状态

git status   查看状态

git status   查看状态

git remote add origin [git\@github.com](mailto:git@github.com):fudaowei/github-study.git   添加远程地址，并取名为origin

git remote add origin [git\@github.com](mailto:git@github.com):fudaowei/github-study.git   添加远程地址，并取名为origin

git remote add origin [git\@github.com](mailto:git@github.com):fudaowei/github-study.git   添加远程地址，并取名为origin

git remote add origin [git\@github.com](mailto:git@github.com):fudaowei/github-study.git   添加远程地址，并取名为origin

git remote add origin [git\@github.com](mailto:git@github.com):fudaowei/github-study.git   添加远程地址，并取名为origin

git branch -M main   将当前分支强制命名为main

git branch -M main   将当前分支强制命名为main

git branch -M main   将当前分支强制命名为main

git branch -M main   将当前分支强制命名为main

git branch -M main   将当前分支强制命名为main

git push -u origin main   将当前分支推送到远程仓库，并建立追踪关系

git puah -u origin main   将当前分支推送到远程仓库，并建立追踪关系

git push -u origin main   将当前分支推送到远程仓库，并建立追踪关系

git push -u origin main   将当前分支推送到远程仓库，并建立追踪关系

git push -u origin main   将当前分支推送到远程仓库，并建立追踪关系

git diff   查看改动

git diff   查看改动

git diff   查看改动

cat > .gitignore << ‘EOF’   创建.gitignore文件

node_modules/

.env

EOF

cat > .gitignore << ‘EOF’   创建.gitignore文件

node_modules/

.env

EOF

cat > .gitignore << ‘EOF’   创建.gitignore文件

node_modules/

.env

EOF

cat > .gitignore << ‘EOF’   创建.gitignore文件

node_modules/

.env

EOF

cat > .gitignore << ‘EOF’   创建.gitignore文件

node_modules/

.env

EOF

git clone xxx   克隆仓库

git clone xxx   克隆仓库

git clone xxx   克隆仓库

git log --oneline -20   查看最近20条记录

git log --oneline -20   查看最近20条记录

git log --oneline -20   查看最近20条记录

git log --oneline -20   查看最近20条记录

git log --oneline -20   查看最近20条记录

git log --graph --oneline --all   图形化查看分支结构

git log --graph --oneline --all   图形化查看分支结构

git log --graph --oneline --all   图形化查看分支结构

git log --graph --oneline --all   图形化查看分支结构

git log --graph --oneline --all   图形化查看分支结构

ls   查看文件数量

ls   查看文件数量

ls   查看文件数量

git checkout -b xxx   创建并切换到新分支

git checkout -b xxx   创建并切换到新分支

git checkout -b xxx   创建并切换到新分支

git checkout -b xxx   创建并切换到新分支

git checkout -b xxx   创建并切换到新分支

git branch   查看分支情况

git branch   查看分支情况

git branch   查看分支情况

git checkout main   切换为main分支

git checkout main   切换为main分支

git checkout main   切换为main分支

git checkout main   切换为main分支

git checkout main   切换为main分支

git merge xxx   将xxx分支的内容合并进当前分支

git merge xxx   将xxx分支的内容合并进当前分支

git merge xxx   将xxx分支的内容合并进当前分支

git merge xxx   将xxx分支的内容合并进当前分支

git merge xxx   将xxx分支的内容合并进当前分支

git branch -d xxx   安全删除xxx分支

git branch -d xxx   安全删除xxx分支

git branch -d xxx   安全删除xxx分支

git branch -d xxx   安全删除xxx分支

git branch -d xxx   安全删除xxx分支

git branch -d xxx   安全删除xxx分支

git branch -D xxx   强制删除xxx分支







